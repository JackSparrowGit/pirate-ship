# 服务注册中心
spring:
  application:
    name: pirate-eureka-server

info:
  app:
    name: ${spring.application.name}
    description: EUREKA Application
    version: #project.version#
  project:
    version: #project.version#