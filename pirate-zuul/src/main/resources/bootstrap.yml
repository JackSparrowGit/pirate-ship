spring:
  application:
    name: pirate-zuul


info:
  app:
    name: ${spring.application.name}
    description: GATEWAY Application
    version: #project.version#
  project:
    version: #project.version#
