spring:
  application:
    name: pirate-cms-web
info:
  app:
    name: ${spring.application.name}
    description: CMS Application
    version: #project.version#
  project:
    version: #project.version#