spring:
  application:
    name: pirate-gateway


info:
  app:
    name: ${spring.application.name}
    description: GATEWAY Application
    version: #project.version#
  project:
    version: #project.version#
